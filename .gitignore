# Built plugin output (generated by scripts/build-plugin.sh)
dist/

# The filled-in, project-specific instructions (keep the template, not the copy)
PROJECT.md

# Per-project Claude Code local overrides & state
.claude/settings.local.json
.claude/agent-memory-local/
.agent-locks/
.worktrees/

# Env & secrets — never commit
.env
.env.*
!.env.example
*.pem
id_rsa*
secrets/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies / build artifacts (uncomment per project as needed)
# node_modules/
__pycache__/
*.pyc
# target/
# bin/
# obj/
